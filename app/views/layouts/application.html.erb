<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= render 'layouts/authentication_box' %>
      <%= content_tag :h1, yield(:title) if show_title? and not @no_title%>
      <%= yield %>
      <div class="footer">
        Powered by <a href="http://github.com/mifo/Meetr">Meetr</a> &copy; 2010 Michal Fojtik
      </div>
    </div>
  </body>
</html>
